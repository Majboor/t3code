{
  "formatVersion": "2.0",
  "identity": {
    "id": "pack_c7f9cb58f6a544e7b16c",
    "name": "ssh-deploy",
    "version": "1.6.0",
    "displayName": "ssh-deploy",
    "summary": "Deploy a thing to a server over SSH, whatever shape the thing is",
    "publisher": {
      "type": "user",
      "handle": "t3demo",
      "displayName": "t3demo"
    },
    "license": "UNLICENSED",
    "tags": [
      "deploy",
      "ssh",
      "tui",
      "web",
      "documents"
    ]
  },
  "provenance": {
    "workspace": {
      "workspaceKeyId": "wsk_2fadd5dfb56c40d6bd28"
    },
    "extractedAt": "2026-08-08T20:14:09.879Z",
    "extractedBy": {
      "type": "user",
      "displayName": "t3demo"
    },
    "handover": {
      "path": "handover.md",
      "summary": "Was ssh-flask-deploy; renamed and re-scoped at 1.0.0 because none of what it learned was ever Flask-specific. Ten real deployments to a shared Ubuntu 24.04.2 host, driven by three accounts through the product's own deploy CLI, plus a document service. Five things bit, all recorded: the server dying with the SSH session, a port already taken, macOS tar headers that make GNU tar warn on a good extraction, pkill -f killing the deploy session because the pattern matched its own command line, and a health check answered by the previous deployment so three deploys reported success while shipping nothing. Added at 1.0.0: what to do when the thing is not web-shaped. A terminal program has no port and cannot pass a health check, so the pack tells the agent to stop and agree a front end rather than ship something nobody can reach. Left undone: no TLS, no restart-on-boot unit, no rollback, and the front-end-for-a-TUI path is written down but has not yet been walked end to end.",
      "generatedAt": "2026-08-08T20:14:09.879Z"
    }
  },
  "capability": {
    "does": "Ships a project to a host over SSH and keeps it running — a web app, a document, or a terminal program that needs a front end built for it first."
  },
  "knowledge": {
    "failureModes": [
      {
        "id": "ssh-exit-kills-the-service",
        "symptom": "The deploy exits zero and the port answers during the run, then nothing is listening a minute later.",
        "trigger": "gunicorn is started in the SSH command without detaching it, so it dies with the session that launched it.",
        "attributedTo": {
          "kind": "host",
          "detail": "The SSH server reaps the process group when the session closes."
        },
        "severity": "critical",
        "silent": true,
        "detection": {
          "signal": "healthcheck",
          "match": "connection refused on the deploy port after the deploy reported success"
        },
        "resolution": {
          "kind": "fixed",
          "inVersion": "0.1.0",
          "change": "The start command runs under nohup with its pid written to app.pid, and health is checked from a separate SSH session so a reaped process cannot pass."
        },
        "firstSeenAt": "2026-08-08T20:30:00.000Z",
        "deploymentsAffected": 1,
        "conditions": {
          "observedAt": "2026-08-08T20:30:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "Ubuntu 24.04.2 shared host"
            }
          ]
        },
        "origin": {
          "introducedIn": "0.1.0",
          "source": {
            "kind": "human-report"
          },
          "recordedAt": "2026-08-08T20:30:00.000Z"
        }
      },
      {
        "id": "port-already-in-use-on-a-shared-host",
        "symptom": "gunicorn exits immediately with 'Address already in use' after the files are already on the host.",
        "trigger": "A host that already runs other services has the chosen port taken; the check has to happen before shipping, not after.",
        "attributedTo": {
          "kind": "host",
          "detail": "Verified on a host already running gunicorn on 18787, garage and several cloudflared tunnels."
        },
        "severity": "high",
        "detection": {
          "signal": "log-pattern",
          "match": "Address already in use"
        },
        "resolution": {
          "kind": "fixed",
          "inVersion": "0.1.0",
          "change": "preflightCommand checks the port is free on the host before any file is shipped."
        },
        "firstSeenAt": "2026-08-08T20:30:00.000Z",
        "deploymentsAffected": 1,
        "conditions": {
          "observedAt": "2026-08-08T20:30:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "Ubuntu 24.04.2 shared host"
            }
          ]
        },
        "origin": {
          "introducedIn": "0.1.0",
          "source": {
            "kind": "human-report"
          },
          "recordedAt": "2026-08-08T20:30:00.000Z"
        }
      },
      {
        "id": "binding-every-interface-publishes-the-app",
        "symptom": "The app is reachable from the public internet straight after a deploy nobody meant to expose.",
        "trigger": "Binding 0.0.0.0 rather than 127.0.0.1 on a host with a public address.",
        "attributedTo": {
          "kind": "pack"
        },
        "severity": "high",
        "silent": true,
        "detection": {
          "signal": "log-pattern",
          "match": "Listening at: http://0.0.0.0"
        },
        "resolution": {
          "kind": "fixed",
          "inVersion": "0.1.0",
          "change": "The start command binds 127.0.0.1 explicitly; publishing is left to the tunnel or proxy the host already runs."
        },
        "firstSeenAt": "2026-08-08T20:30:00.000Z",
        "deploymentsAffected": 1,
        "conditions": {
          "observedAt": "2026-08-08T20:30:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "Ubuntu 24.04.2 shared host"
            }
          ]
        },
        "origin": {
          "introducedIn": "0.1.0",
          "source": {
            "kind": "human-report"
          },
          "recordedAt": "2026-08-08T20:30:00.000Z"
        }
      },
      {
        "id": "pkill-f-matches-the-deploy-session-itself",
        "symptom": "The deploy exits 255 with nothing in its output but tar warnings, and the app is never restarted.",
        "trigger": "Stopping the previous run with `pkill -f` on a pattern naming the start command. The remote shell's own command line contains that command, so the pattern matches the session running the deploy and kills it mid-way.",
        "attributedTo": {
          "kind": "host",
          "detail": "pkill -f matches against the full command line of every process, including the ssh session carrying the deploy."
        },
        "severity": "high",
        "silent": false,
        "detection": {
          "signal": "log-pattern",
          "match": "exit 255 with no command output after the extraction"
        },
        "resolution": {
          "kind": "fixed",
          "inVersion": "0.2.0",
          "change": "Stop the previous run by the pid recorded in app.pid instead of by command-line pattern."
        },
        "firstSeenAt": "2026-08-09T00:50:00.000Z",
        "lastSeenAt": "2026-08-09T00:50:00.000Z",
        "deploymentsAffected": 1,
        "conditions": {
          "observedAt": "2026-08-09T00:50:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "Ubuntu 24.04.2 shared host"
            }
          ]
        },
        "origin": {
          "introducedIn": "0.2.0",
          "source": {
            "kind": "deployment-telemetry",
            "signal": "deploy.run.completed"
          },
          "recordedAt": "2026-08-09T00:50:00.000Z"
        }
      },
      {
        "id": "health-check-answered-by-the-previous-deployment",
        "symptom": "The deploy reports success and the site keeps serving the version before it. Every check that the new change is live fails, with nothing in the deploy output to explain why.",
        "trigger": "A process from an earlier deployment still holds the port. The new gunicorn cannot bind and exits, but `curl /healthz` is answered by the old one, so a health check that only asks whether something is listening passes against the wrong process.",
        "attributedTo": {
          "kind": "pack"
        },
        "severity": "critical",
        "silent": true,
        "detection": {
          "signal": "healthcheck",
          "match": "health passes but the served build id is not the one just shipped"
        },
        "resolution": {
          "kind": "fixed",
          "inVersion": "0.3.0",
          "change": "Each deploy writes a BUILD file, the app reports that id from /healthz, and the deploy demands the id it just shipped. The port is also freed by whoever holds it (fuser -k PORT/tcp) rather than by matching a command line."
        },
        "firstSeenAt": "2026-08-09T01:55:00.000Z",
        "lastSeenAt": "2026-08-09T01:55:00.000Z",
        "deploymentsAffected": 3,
        "conditions": {
          "observedAt": "2026-08-09T01:55:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "Ubuntu 24.04.2 shared host"
            }
          ]
        },
        "origin": {
          "introducedIn": "0.3.0",
          "source": {
            "kind": "deployment-telemetry",
            "signal": "deploy.run.completed"
          },
          "recordedAt": "2026-08-09T01:55:00.000Z"
        }
      },
      {
        "id": "appledouble-files-ride-along-from-macos",
        "symptom": "Deployed directories fill with `._app.py`, `._BUILD` and friends that no redeploy ever clears.",
        "trigger": "rsync from a macOS working copy carries the resource-fork sidecar files across. `--delete` does not remove them, because every deploy re-sends them, so they accumulate on a host whose disk is already shared.",
        "attributedTo": {
          "kind": "host",
          "detail": "macOS writes AppleDouble sidecars beside files on non-native filesystems, and rsync treats them as ordinary files to ship."
        },
        "severity": "low",
        "silent": true,
        "detection": {
          "signal": "log-pattern",
          "match": "._ prefixed files listed in the deploy's rsync output"
        },
        "resolution": {
          "kind": "fixed",
          "inVersion": "1.4.0",
          "change": "The ship step excludes them: `rsync -a --delete --exclude='._*'`. Deployments made before this still hold theirs, because no later deploy will clear them."
        },
        "firstSeenAt": "2026-08-15T06:53:00.000Z",
        "lastSeenAt": "2026-08-15T06:53:00.000Z",
        "deploymentsAffected": 5,
        "conditions": {
          "observedAt": "2026-08-15T06:53:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "Ubuntu 24.04.2 shared host"
            },
            {
              "axis": "deployment-target",
              "value": "macOS author machine"
            }
          ]
        },
        "origin": {
          "introducedIn": "1.4.0",
          "source": {
            "kind": "human-report"
          },
          "recordedAt": "2026-08-15T19:45:00.000Z"
        }
      },
      {
        "id": "nothing-retires-the-previous-deployment",
        "symptom": "A shared host accumulates deployment directories and detached processes holding loopback ports, with no record of which are still wanted.",
        "trigger": "The deploy only ever adds: it ships a new directory and starts a new process without stopping or removing the one it replaces. The `app.pid` files it writes were empty, so even a cleanup that trusted them had no handle to use.",
        "attributedTo": {
          "kind": "pack"
        },
        "severity": "medium",
        "silent": true,
        "detection": {
          "signal": "healthcheck",
          "match": "more directories under the remote dir than deployments anyone can name"
        },
        "resolution": {
          "kind": "fixed",
          "inVersion": "1.4.0",
          "change": "The deploy retires what it replaces: stop the previous process by pid (falling back to the process whose cwd is the old directory), delete the old directory, and verify app.pid is non-empty and live before reporting success."
        },
        "firstSeenAt": "2026-08-09T02:22:00.000Z",
        "lastSeenAt": "2026-08-15T20:00:00.000Z",
        "deploymentsAffected": 6,
        "conditions": {
          "observedAt": "2026-08-15T20:00:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "Ubuntu 24.04.2 shared host"
            }
          ]
        },
        "origin": {
          "introducedIn": "1.4.0",
          "source": {
            "kind": "human-report"
          },
          "recordedAt": "2026-08-15T20:10:00.000Z"
        }
      },
      {
        "id": "a-hand-run-deploy-is-invisible-to-the-project",
        "symptom": "The app is live and serving, but the project's Infrastructure page says nothing is deployed and analytics has nothing to attach numbers to.",
        "trigger": "Following the deploy steps literally by shelling out rsync and gunicorn. That puts the process on the host without telling T3, which only learns about a deployment through `t3 deploy run`.",
        "attributedTo": {
          "kind": "pack"
        },
        "severity": "high",
        "silent": true,
        "detection": {
          "signal": "reconciliation-mismatch",
          "match": "a reachable deployment with no row on the project's infrastructure page"
        },
        "resolution": {
          "kind": "fixed",
          "inVersion": "1.4.0",
          "change": "The prompt registers the start command as a deploy target and runs it through `t3 deploy run`, which records the run and registers the deployment, and wires analytics with --analytics-stream rather than by hand."
        },
        "firstSeenAt": "2026-08-15T20:20:00.000Z",
        "lastSeenAt": "2026-08-15T20:20:00.000Z",
        "deploymentsAffected": 1,
        "conditions": {
          "observedAt": "2026-08-15T20:20:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "local loopback"
            }
          ]
        },
        "origin": {
          "introducedIn": "1.4.0",
          "source": {
            "kind": "install-report",
            "target": "local"
          },
          "recordedAt": "2026-08-15T20:35:00.000Z"
        }
      },
      {
        "id": "a-foreground-quick-tunnel-never-returns-an-address",
        "symptom": "The deploy finishes with the app listening on loopback, and the reply names no trycloudflare address — so nothing outside the machine can reach what was just deployed.",
        "trigger": "Running `cloudflared tunnel --url` in the foreground. It never exits, so it holds the turn open until something gives up and kills it, and the address it printed is never read back.",
        "attributedTo": {
          "kind": "pack"
        },
        "severity": "high",
        "silent": true,
        "detection": {
          "signal": "log-pattern",
          "match": "no https://<name>.trycloudflare.com address in the deploy's reply while the port answers on 127.0.0.1"
        },
        "resolution": {
          "kind": "fixed",
          "inVersion": "1.5.0",
          "change": "The prompt runs the tunnel detached with its output redirected, polls that file for the trycloudflare line, and requires the address to be reported and passed to `t3 deploy run --url`."
        },
        "firstSeenAt": "2026-08-15T22:45:00.000Z",
        "lastSeenAt": "2026-08-15T22:45:00.000Z",
        "deploymentsAffected": 1,
        "conditions": {
          "observedAt": "2026-08-15T22:45:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "local machine, published by a cloudflare quick tunnel"
            }
          ]
        },
        "origin": {
          "introducedIn": "0.1.0",
          "source": {
            "kind": "human-report"
          },
          "recordedAt": "2026-08-15T22:45:00.000Z"
        }
      },
      {
        "id": "the-ship-deletes-the-pid-file-the-old-process-keeps-the-port",
        "symptom": "A redeploy fails its health check while the old build goes on answering: new files on the host, the port still held by the previous deployment, 'Address already in use' in app.err.",
        "trigger": "`rsync -a --delete` deletes `app.pid` on the host, because the file is written there and is not in what was sent. The start command's only way to stop the previous deployment is that pid, so it stops nothing. From then on it is self-sustaining: the failed run writes its own doomed pid into `app.pid`, and every run after it kills that corpse instead of the listener.",
        "attributedTo": {
          "kind": "pack"
        },
        "severity": "critical",
        "silent": false,
        "detection": {
          "signal": "log-pattern",
          "match": "Address already in use while /healthz still reports the previous build id"
        },
        "resolution": {
          "kind": "fixed",
          "inVersion": "1.6.0",
          "change": "The ship excludes the host's own runtime files (app.pid, app.log, app.err, tunnel.*), and the start command clears whatever holds the port — lsof or fuser — rather than trusting app.pid, which a failed run leaves pointing at a process that never bound."
        },
        "firstSeenAt": "2026-08-17T03:47:44.947Z",
        "lastSeenAt": "2026-08-17T04:10:26.777Z",
        "deploymentsAffected": 2,
        "conditions": {
          "observedAt": "2026-08-17T04:10:26.777Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "Ubuntu 24.04.2 shared host"
            }
          ]
        },
        "origin": {
          "introducedIn": "0.1.0",
          "source": {
            "kind": "human-report"
          },
          "recordedAt": "2026-08-18T08:00:00.000Z"
        }
      }
    ],
    "integration": [
      {
        "id": "classify-the-thing-before-shipping-it",
        "title": "Work out whether the project serves anything before choosing how to deploy it",
        "detail": {
          "kind": "pattern",
          "rule": "Before writing a deploy command, answer one question: when this runs, does anything listen on a port? If yes, ship it and start it. If no, deploying it unchanged starts something nobody can reach. A terminal program is the obvious case, but it is not the only one — a CLI, a library, a batch job, a data pipeline, a scraper, a training script and a cron task all have no web surface either. Do not go looking for the word TUI; look for whether anything opens a socket.",
          "rationale": "Every deploy command here ends in a health check against a port, so a program that never opens one cannot pass. The question is not what kind of program it is, which invites arguing about labels, but whether anything answers when something connects — which is knowable by reading the code before any of the work starts.",
          "example": "serves HTTP (flask, express, a static server) -> ship and start it | produces a file (PDF, report, dataset) -> build it, then serve the file | nothing listens (CLI, TUI, batch job, pipeline, library, cron task) -> it has no port; see the next note"
        },
        "conditions": {
          "observedAt": "2026-08-14T09:00:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "Ubuntu 24.04.2 shared host"
            }
          ]
        },
        "origin": {
          "introducedIn": "1.0.0",
          "source": {
            "kind": "human-report"
          },
          "recordedAt": "2026-08-14T09:00:00.000Z"
        }
      },
      {
        "id": "a-terminal-program-needs-a-front-end-and-a-conversation",
        "title": "Nothing to reach it with, so ask before inventing one",
        "detail": {
          "kind": "wiring",
          "prompt": "Do this before writing any deploy command, and before deciding the task is routine. The most common way this pack is wasted is being read after the plan is already made.\n\nWhen nothing in the project listens on a port — a terminal program, a CLI, a batch job, a pipeline, a library, a scheduled task — stop and talk to the person first. Say plainly: this has no web surface, so deploying it as-is would start something nobody can reach. Then offer what fits what it actually does. A program somebody drives interactively usually wants a small web front end over the same functionality: say which commands become which screens, what the front end will call underneath, and what does not survive the move (raw keyboard handling, curses redraw, anything assuming a live terminal). Something that runs to completion and produces output usually wants scheduling plus a page showing the last run and its result, not a front end at all. A library usually should not be deployed. Say which you think it is and why, get agreement on that shape before building it, then build it, give it a /healthz, and deploy that. Do not silently generate a front end and ship it: the person asked to deploy their program, and what they would get is a different program.",
          "touches": []
        },
        "conditions": {
          "observedAt": "2026-08-14T09:00:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "Ubuntu 24.04.2 shared host"
            }
          ]
        },
        "origin": {
          "introducedIn": "1.0.0",
          "source": {
            "kind": "human-report"
          },
          "recordedAt": "2026-08-14T09:00:00.000Z"
        }
      },
      {
        "id": "bind-loopback-and-front-it",
        "title": "Bind loopback and let a tunnel or proxy publish it",
        "detail": {
          "kind": "pattern",
          "rule": "Bind gunicorn to 127.0.0.1 and expose the app through the tunnel or reverse proxy the host already runs.",
          "rationale": "The host is shared with other services and has a public address, so a bind to 0.0.0.0 publishes the app the moment it starts.",
          "example": "gunicorn -w 2 -b 127.0.0.1:18901 app:app"
        },
        "conditions": {
          "observedAt": "2026-08-08T20:30:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "Ubuntu 24.04.2 shared host"
            }
          ]
        },
        "origin": {
          "introducedIn": "0.1.0",
          "source": {
            "kind": "human-report"
          },
          "recordedAt": "2026-08-08T20:30:00.000Z"
        }
      },
      {
        "id": "detach-the-process-from-the-ssh-session",
        "title": "Detach the server from the SSH session",
        "detail": {
          "kind": "pattern",
          "rule": "Start the server with nohup, record its pid to a file, then verify the health endpoint in a second, separate SSH session.",
          "rationale": "Checking health inside the session that started the process passes even when the process is about to be reaped with that session.",
          "example": "(nohup gunicorn -w 2 -b 127.0.0.1:$PORT app:app > deploy.log 2>&1 & echo $! > app.pid)"
        },
        "conditions": {
          "observedAt": "2026-08-08T20:30:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "Ubuntu 24.04.2 shared host"
            }
          ]
        },
        "origin": {
          "introducedIn": "0.1.0",
          "source": {
            "kind": "human-report"
          },
          "recordedAt": "2026-08-08T20:30:00.000Z"
        }
      },
      {
        "id": "macos-tar-headers-are-not-an-error",
        "title": "Ignore LIBARCHIVE.xattr warnings when shipping from macOS",
        "detail": {
          "kind": "pattern",
          "rule": "Treat 'Ignoring unknown extended header keyword LIBARCHIVE.xattr.*' from GNU tar as noise, not failure.",
          "rationale": "BSD tar on macOS writes Apple xattr headers GNU tar does not know; the extraction is complete and correct, but a deploy script that greps stderr will call a good deploy failed.",
          "example": "tar czf - . | ssh host 'tar xzf - -C /srv/app'"
        },
        "conditions": {
          "observedAt": "2026-08-08T20:30:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "Ubuntu 24.04.2 shared host"
            }
          ]
        },
        "origin": {
          "introducedIn": "0.1.0",
          "source": {
            "kind": "human-report"
          },
          "recordedAt": "2026-08-08T20:30:00.000Z"
        }
      },
      {
        "id": "prove-the-new-build-is-the-one-answering",
        "title": "Make the health check name the build",
        "detail": {
          "kind": "pattern",
          "rule": "Stamp every build with an id, serve that id from the health endpoint, and have the deploy require the id it just shipped.",
          "rationale": "\"Something is listening\" is not \"my change is running\". Without an id the two are indistinguishable, and a deploy that shipped nothing looks exactly like one that worked.",
          "example": "curl -fsS http://127.0.0.1:$PORT/healthz | grep -q \"$(cat BUILD)\""
        },
        "conditions": {
          "observedAt": "2026-08-09T01:55:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "Ubuntu 24.04.2 shared host"
            }
          ]
        },
        "origin": {
          "introducedIn": "0.3.0",
          "source": {
            "kind": "deployment-telemetry",
            "signal": "deploy.run.completed"
          },
          "recordedAt": "2026-08-09T01:55:00.000Z"
        }
      },
      {
        "id": "the-health-check-is-the-contract",
        "title": "Whatever you deploy has to answer a health check",
        "detail": {
          "kind": "pattern",
          "rule": "Give every deployment a /healthz that returns the current build id, whatever the thing is underneath.",
          "rationale": "It is the only way this pack can tell a deploy that worked from one that shipped nothing and left the previous version answering. A document server and a generated front end both need it as much as a web app does.",
          "example": "a PDF service: /healthz -> {\"status\":\"ok\",\"build\":\"<id>\",\"pages\":4}"
        },
        "conditions": {
          "observedAt": "2026-08-14T09:00:00.000Z",
          "other": [
            {
              "axis": "deployment-target",
              "value": "Ubuntu 24.04.2 shared host"
            }
          ]
        },
        "origin": {
          "introducedIn": "1.0.0",
          "source": {
            "kind": "human-report"
          },
          "recordedAt": "2026-08-14T09:00:00.000Z"
        }
      }
    ]
  },
  "requirements": {
    "environment": [
      {
        "name": "DEPLOY_SSH_HOST",
        "purpose": "Host the app is shipped to. The LogicPacks deployment node.",
        "secret": false,
        "required": true,
        "example": "164.68.117.31"
      },
      {
        "name": "DEPLOY_SSH_USER",
        "purpose": "SSH user with write access to the remote directory.",
        "secret": false,
        "required": true,
        "example": "root"
      },
      {
        "name": "DEPLOY_SSH_PASSWORD",
        "purpose": "SSH password, read from the server secret store rather than argv.",
        "secret": true,
        "required": true
      },
      {
        "name": "DEPLOY_REMOTE_DIR",
        "purpose": "Directory on the host the app is unpacked into. One flat directory per deployment.",
        "secret": false,
        "required": true,
        "example": "/srv/t3-pack-1786667796599"
      },
      {
        "name": "DEPLOY_PORT",
        "purpose": "Loopback port the app binds. Must be free on the host — check with `ss -ltn` before taking it, because the host runs unrelated services on nearby ports.",
        "secret": false,
        "required": true,
        "example": "19400"
      },
      {
        "name": "DEPLOY_START_COMMAND",
        "purpose": "The command that starts this project on the host. Comes from the project, not from this pack.",
        "secret": false,
        "required": true,
        "example": "gunicorn -w 2 -b 127.0.0.1:$DEPLOY_PORT app:app"
      }
    ],
    "toolchain": [
      {
        "name": "ssh"
      },
      {
        "name": "sshpass"
      },
      {
        "name": "tar"
      },
      {
        "name": "curl"
      }
    ],
    "preflightCommand": "command -v sshpass ssh tar >/dev/null && ssh -o BatchMode=no -o StrictHostKeyChecking=no \"$DEPLOY_SSH_USER@$DEPLOY_SSH_HOST\" \"command -v gunicorn python3 >/dev/null && ! ss -ltn | grep -q ':$DEPLOY_PORT '\""
  },
  "runtime": {
    "target": "none",
    "commands": {
      "start": {
        "command": "${DEPLOY_START_COMMAND}",
        "cwd": ".",
        "description": "Whatever starts this project, bound to a loopback port."
      },
      "healthcheck": {
        "command": "curl -fsS http://127.0.0.1:${DEPLOY_PORT}/healthz | grep -q \"$(cat BUILD)\"",
        "description": "Exits zero only when the build just shipped is the one answering."
      }
    }
  },
  "permissions": {
    "acceptsInboundNetwork": true,
    "secrets": [
      {
        "name": "DEPLOY_SSH_PASSWORD",
        "purpose": "Authenticate the SSH session that ships and starts the app."
      }
    ]
  },
  "verification": {
    "record": {
      "measuredAt": "2026-08-14T09:00:00.000Z",
      "installsAttempted": 4,
      "installsSucceeded": 4,
      "deploymentsAttempted": 10,
      "deploymentsSurviving": 7,
      "cumulativeServiceDays": 0,
      "breakagesCaught": 5,
      "breakagesFixed": 5
    }
  },
  "visibility": {
    "scope": "workspace",
    "workspaceKeyId": "wsk_2fadd5dfb56c40d6bd28"
  },
  "integration": {
    "prompt": "Deploy this project, either to this machine or to a host over SSH.\n\nAsk T3 what it already knows before you ask the person anything: `t3 deploy list --project <projectId>` names every target this project has, each with the start command it runs and, for an ssh target, its host, user and remote path. That listing is the memory of the last deploy — there is no config file beside the project to read, and one written there would only be a second copy of this to disagree with.\n\nIf a target is listed, this is a redeploy: reuse it by name and ask nothing. If none is, this is the first deploy: ask for what you need — which target, and for an ssh target the host, user, remote directory, port range and the *name* of the secret holding the password — then register it as a target (step 7) so the next deploy finds it. Never write the password into a file or pass it on a command line.\n\nTwo targets:\n- `local` -> run the start command on this machine.\n- `ssh` -> ship it to the host and start it there.\n\nEither way, publish with a cloudflare quick tunnel once it is listening:\n`cloudflared tunnel --url http://127.0.0.1:$DEPLOY_PORT`.\n\nRun it **detached, with its output redirected to a file**, and read the address back out of that file — `nohup cloudflared tunnel --url http://127.0.0.1:$DEPLOY_PORT > tunnel.log 2>&1 &`, then poll `tunnel.log` for the `https://….trycloudflare.com` line. The command never exits: run it in the foreground and it holds the session open until something gives up and kills it, which is how a deploy ends with the app listening on loopback and no address to hand back. Capture the address, because it *is* the deployment's address and it changes every time the tunnel restarts. Pass it to `t3 deploy run --url`, and say it in your reply — a tunnel nobody wrote down is a deployment nobody can reach.\n\nFirst work out what the project is, because that decides everything after:\n- It already serves HTTP -> ship it and start it.\n- It produces a document -> build the document, then serve the file.\n- It is a terminal program -> it has no port and cannot pass a health check. Stop and talk to the person: offer a small web front end over the same functionality, say which commands become which screens and what will not survive the move, and agree that shape before building it. Do not ship a front end nobody asked for.\n\nThen, whatever the shape:\n1. Preflight before shipping anything: the tools exist locally, the host has what the start command needs, and nothing is already on DEPLOY_PORT — *including anything you started yourself*. If you ran the server by hand to try it, stop it before the deploy: the port it is holding is the one the deploy needs, and the deploy is the thing that fails. The port that has to be free is the one on the machine the start command runs on: for an `ssh` target that is the host, and the same number on your own machine is somebody else's — a forward, another project, the person's own browser — and not yours to clear.\n   Capture the start command's errors while you are at it. `gunicorn --daemon` detaches its own stdio, so a bind failure is written nowhere: the run exits non-zero with an empty log and nothing to say why. Add `--error-logfile <file>` (or start it without `--daemon` and redirect), or you will be debugging a silent failure.\n2. Stamp a build id, and serve it from /healthz.\n3. Ship the directory with `rsync -a --delete --exclude='._*'` — macOS resource forks otherwise ride along and `--delete` will never clear them — and start the process detached, with its pid written down.\n   Exclude the host's own runtime files from that ship: `--exclude='app.pid' --exclude='app.log' --exclude='app.err' --exclude='tunnel.*'`. They exist only on the host, so `--delete` removes them for not being in what you sent — and `app.pid` is the running deployment's only handle. Delete it and the next start command has nothing to stop, the old process keeps the port, and the new one dies on `Address already in use` while the old build goes on answering.\n4. Verify from a separate session, and require the build id you just shipped — not merely that something answers, which the previous deployment will happily do.\n5. Retire what you replaced. Stop the previous deployment of this project *before* starting the new one — by the pid in its `app.pid`, and if that file is empty or missing, by finding the process whose working directory is the old deployment directory (`ls -l /proc/*/cwd`) rather than by `pkill -f`, which matches the deploy session itself. Then delete the old directory. A deploy that only adds leaves a shared host accumulating directories and detached processes holding ports nobody can account for, and that is exactly what happened before this step existed.\n   The start command itself has to do the stopping, and it cannot trust `app.pid` to be the whole story: a run that failed after writing the file left it naming a process that never bound, so every run after it kills a corpse and leaves the real listener alone. Fall back to whatever holds the port before binding — `lsof -tiTCP:$DEPLOY_PORT -sTCP:LISTEN` or `fuser -k -n tcp $DEPLOY_PORT` — and wait for it to go. Both of the redeploys in the run that found this failed their first attempt on exactly that, and each one cost a person a manual round on the host to clear.\n6. Write the pid down and check you wrote it. An empty `app.pid` is the same as no handle at all: verify the file is non-empty and names a live process before reporting success.\n\n7. Record it, or the project cannot see it. A deploy you ran by hand is invisible to T3: nothing appears on the project's Infrastructure page and there is no deployment for numbers to attach to. Register the start command once as a target and run it through T3 rather than executing it yourself:\n     `t3 deploy add --project <projectId> --name <name> --command '<the whole start command>'`\n     `t3 deploy run <targetId> --url <the address it is reachable on>`\n   Run it that way every time. `deploy run` executes the command from the project directory, records the run, and registers what went live.\n   Keep the name stable across deploys — it is how this deployment is identified. Registering a name that is already there re-registers that same target, so a redeploy replaces what is live; inventing a new name each time stands up a rival deployment beside it, and the next deploy that wires analytics is refused for taking the stream away from what is really its own past self.\n8. When it should report numbers, let the deploy wire them. Add `--analytics-stream <name>` (with `--analytics-purpose` and `--analytics-properties` the first time) to `t3 deploy run`. The deploy declares the stream if needed, mints its ingest key, injects it into the process it starts as `T3_ANALYTICS_INGEST_KEY`, and links the deployment to the stream. Read the key from the environment in the deployed code and never write one into a file — a key committed beside the project is a key anybody can post with.\n\nBind loopback and let the tunnel publish it. Read the SSH password from the server secret store, never from a command line."
  },
  "contents": {
    "readmePath": "README.md",
    "files": [
      {
        "path": "README.md",
        "sha256": "b0a74a1101815b365b6b0437ce4bc37b2c5ead9416c47d51c9c04aa65c290a4f",
        "bytes": 10650
      },
      {
        "path": "handover.md",
        "sha256": "f44dff5f361a80d2c36f9dd51c085e6337d18a7ca111ca16caa0f3d43a316463",
        "bytes": 6193
      }
    ]
  },
  "signature": {
    "keyId": "472393d85b830fe4fa2d99ceabc8f51a",
    "algorithm": "ed25519",
    "publicKey": "MCowBQYDK2VwAyEA1bCFimWtyklfp93IJQhzKONqPlG/0FeRBAIgKtPl9QE=",
    "manifestSha256": "b9c3144fa032c1a4a77a2d2a49916040ef58ebb8e0b83012dce709b31dd3faeb",
    "signature": "hWpnDGY9tZBZwCPrZgU2CdJC6FgFajPaLCjODfp2Un5vSQL1+7kEHGfES3qaBJJjQrfGdjg9va+BHNsbu+XyCQ==",
    "signedAt": "2026-08-18T08:15:28.576Z"
  }
}
